var vowel = ["a", "A", "e", "E", "i", "I", "o", "O", "u", "U"]

var y = ["y", "Y"]

for (i = 0; (vowels.includes(i)) === false; i += 1) {
  var slicedLetter = word.slice(i);
  alert(slicedLetter);
  word = word.append(slicedLetter);
  alert(word);
}

for (i = 0; i < vowels.length; i++) {
  if (vowels.includes[i]) {
    return [i]

    for (i = 0; i < word.length; i++) {
      //console.log(word[i]);
      if (word.includes(queue)) {
        var slicePartOne = word.slice(0, i);
        var slicePartTwo = word.slice(i);
        concatWord = slicePartTwo.concat(slicePartOne);

        // for (i = 0; i < word.length; i++) {
        //   if (queue.includes(word[i])) {
        //     var slicePartOne = word.slice(0, i);
        //     var slicePartTwo = word.slice(i);
        //     var concatWord = slicePartTwo.concat(slicePartOne);
        //     alert(concatWord);

if (word.includes(queue)=== true){
  var u = indexOf(queue)
  word.split(u+1)
}
